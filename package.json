{
  "name": "booking-room",
  "version": "0.0.1",
  "description": "Booking homes manager ",
  "main": "server.js",
  "author": "LL",
  "scripts": {
    "test": "NODE_ENV=test  mocha --require co-mocha --timeout 30000 test/ '**/*.spec.js'",
    "coverage": "NODE_ENV=test istanbul cover _mocha -- --require co-mocha --timeout 20000  test/ 'app/**/*.spec.js'",
    "start": "node server.js",
    "debug": "node --inspect server.js",
    "lint": "eslint src --quiet"
  },
  "dependencies": {
    "body-parser": "^1.18.2",
    "crypto": "^1.0.1",
    "express": "^4.10.8",
    "express-session": "^1.15.6",
    "method-override": "^2.3.10",
    "mongoose": "4.10.8",
    "morgan": "^1.9.0",
    "passport": "^0.4.0",
    "passport-local": "^1.0.0"
  },
  "devDependencies": {
    "co": "^4.6.0"
  }
}
